# Manuel Roca Lima
## Future civil engineering student. 
### <span style="color:blue">-I write code to understand systems.-</span> 

Recursive problem decomposition — I solve problems by reducing them to their trivial case. Binary search, backtracking— all built from scratch without libraries.
Sorting algorithms — Selection, QuickSort implemented with real comparison and swap counters. I measure, I don't assume.
Data structures — Circular queues, search trees in Python and TypeScript, with GUI visualization using Tkinter and canvas geometry.

Parallel ETL pipelines — CSV ingestion into MySQL, split intohunks dispatched across CPU threads. Timed per batch.
Change detection & deduplication — MD5 hashing per record in staging layer. Historical table only receives net-new rows via on hash — no primary key dependency, full audit trail.

